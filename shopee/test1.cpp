#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /**
     * Note: 类名、方法名、参数名已经指定，请勿修改
     *
     * 
     * 访问的结果为int一维数组，含两位数字，第一位数字为平方根整数部分，第二位数字为余数部分
     * @param x int整型  x是一个正整数
     * @return int整型vector
     */
    using ll = long long;
    vector<int> mysqrt(int x) {
        // write code here
        for(ll i = 1; i < LONG_MAX; ++i) {
            
        }
    }
};

int main() {
    int N, M;

    cin >> N >> M;
    vector<vector<char>> grid(N, vector<char>(M));
    pair<int, int> start, end;
    vector<pair<int, int>> portals;
    
    
    return 0;
}
